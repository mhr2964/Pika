# Pika local environment template
# Copy the values you need into local env files for the canonical apps below.
# Do not commit secrets.

# Canonical frontend app: frontend/
VITE_API_BASE_URL=http://localhost:3001

# Canonical backend app: backend/
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
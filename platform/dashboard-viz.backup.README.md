# Dashboard payload backup note

This directory contains a platform-owned backup copy of the latest available dashboard payload observed at:

- `workspace/pulse/viz.json`

## Important
- `dashboard-viz.backup.json` is **not** the live canonical dashboard artifact.
- It exists only to preserve the latest visible payload while the company-root dashboard publication path mismatch remains unresolved.
- The practical currently supported pulse-owned publication path observed in the workspace is `workspace/pulse/viz.json`.